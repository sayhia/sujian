# 2026-08-29

## 仓库迁移到 sayhia/sujian

用户执行 git 推送命令，将项目从 sunmking/panda-time-note 迁移到 sayhia/sujian（新 origin）。
- `git remote set-url origin` 替换为 `git@github.com:sayhia/sujian.git`（原 add 会失败，因 origin 已存在指向 panda-time-note）
- 提交全部 99 项改动为一个 commit（1377300：自建自动更新 + GitHub Actions CI/CD + 前端纸墨重构整合），amend 清理了误入的 4 个 vite 临时文件（`frontend/vite.config.ts.timestamp-*.mjs`，并补 .gitignore）
- `git push -u origin main` 成功，main 跟踪 origin/main

**遗留待修正**：`backend/updater/updater.go` 的 `ManifestURL` 仍硬编码旧仓库 `sunmking/panda-time-note`，应改为 `sayhia/sujian`（或用 ldflags `-X` 注入，与 Version 同理）。否则应用内「检查更新」会请求错误仓库的 latest.json。release.yml 的 publish job 用 `$GITHUB_REPOSITORY` 动态获取仓库地址，无此问题；CI Taskfile 的 BUILD_FLAGS 只注入了 `updater.Version`，未注入 `ManifestURL`。

## 初始化首次 release

用户要求"初始化提交 release"。已：
- 修正 ManifestURL 指向 sayhia/sujian（commit 8f7010f，push main）
- 打首个 release tag `v0.1.0` 并推送 → 触发 release.yml workflow（五平台构建，当前 queued）
- 删除项目自带的过时 `.github/workflows/build.yml`（wails 模板"Build + Test v3"，go 1.24 + 旧 wails3 build 语法，与 ci.yml/release.yml 重叠且每次 failure，commit c3b6fbb）

**关注**：release workflow 首次跑可能因 macos-13(Intel) / ubuntu-24.04-arm runner 可用性、wails3 CLI 安装、跨平台 CGO 工具链等失败，需看 https://github.com/sayhia/sujian/actions 日志逐项调整。仓库现有 3 个 workflow：CI / Release / (build.yml 已删)。
