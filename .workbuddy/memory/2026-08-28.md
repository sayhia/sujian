# 2026-08-28

## 素笺完全重构（前后端 + UI 全新设计）

用户要求"完全重构"，确认范围：整个项目重构 + UI/UX 全新设计 + 一次性重写后整体验证。

**设计**：docs/plans/2026-08-28-full-rewrite-design.md —— "纸墨 Paper & Ink" 设计语言（暖纸白底 + 墨色文字 + 6 种墨色主题），CSS 变量令牌体系 tokens.css/themes.css（data-theme × data-accent 组合，零重载切主题）。

**后端**（保持 API/schema 兼容，bindings 未重新生成）：
- 重写 services/note_service.go：统一 AppError 错误体系、SQL 条件构造收敛（FTS/LIKE 分支）、分页常量、ResetAllData 事务化、草稿 2MiB 上限、PurgeDeleted 语义保留。
- go vet / go test 通过。

**前端**（完全重写，旧组件全删）：
- 旧 3398 行 MarkdownEditor、3138 行 SettingsPanel、1045 行 NoteEditor 全部拆解。
- 新结构：styles(tokens/themes/base) + services(API 层) + stores(notes/ui/settings) + composables(useMarkdown 用 marked v17 renderer 高亮) + components/ui·layout·notes·search·editor·settings + views(HomeView/EditorView)。
- 移除 demo 路由、移除 Tailwind 依赖、清空旧测试重写 11 个单测。
- 功能保留：CRUD/筛选/搜索(FTS5 高亮)/标签/归档/回收站/设置(6 墨色·亮暗·字号·毛玻璃·动画·时间格式·语言)/导入导出/清空/草稿恢复/自动保存/快捷键/统计。
- vue-tsc 0 错误、vite build 通过、vitest 11 通过。

**验证**：go build 产出 bin/sujian(21M)；vite dev :34115 已起用于浏览器预览（Wails 绑定需桌面环境）。

**坑**：marked v17 无 highlight 选项 → 用 marked.use({renderer.code})；vue-tsc 1.8 对 generic 组件支持差 → Segmented 改非泛型；tsconfig lib 需 DOM.Iterable。

## 追加：人民币主题系统

用户要求项目定位"小而精的本地写作平台"，主题按最新版人民币颜色设计。

- AccentKey 重构为 cny-1/5/10/20/50/100（壹圆橄榄绿/伍圆紫韵/拾圆藏蓝/贰拾圆棕黄/伍拾圆翠绿/壹佰圆中国红），themes.css 六套 light/dark 色板。
- 设置面板主题选择器改为"人民币纸币卡片"（面额 ¥ 数字 + 色条 + 主题名 + 背面景观·花卉小字）。
- 旧主题 key 自动迁移（purple→cny-5, blue→cny-10, green→cny-1, teal→cny-50, brown→cny-20, red→cny-100），默认改为 cny-100 中国红；main.ts / index.html 首帧脚本同步迁移逻辑。
- 4 语言 i18n 增加 accents 主题名与花卉描述。
- 构建 + 11 单测通过。

## 追加：主题名称去人民币化

用户要求"主题名称不要出现人民币相关"：配色仍取自人民币色系，但命名与内部 key 全部中性化。

- AccentKey：cny-1/5/10/20/50/100 → olive/violet/navy/amber/emerald/crimson（橄榄绿/紫韵/藏蓝/棕黄/翠绿/中国红）。
- 主题选择器卡片去掉 ¥ 面额与花卉意象，改为"色环 + 主题名 + 气质词"（如 中国红 · 热烈庄重），2 列卡片布局。
- 迁移映射覆盖两层历史 key：purple 等旧色名 + cny-x 面额版，均平滑映射到新 key；默认仍为中国红。
- 4 语言 i18n 同步更新；构建 + 11 单测通过。
- 用户偏好：主题命名不要出现人民币/面额字样（已记入项目约定）。

## 追加：交互与样式精细化打磨（第三轮）

- 全局质感：细滚动条（hover 显现）、accent 选中/焦点环、路由页面过渡（fade+up）、主题切换 420ms 颜色过渡（.theme-anim）、tokens 新增 --shadow-hover/--shadow-pop/--ease-spring/--dur-slower。
- 首页：Skeleton 骨架屏（shimmer）替代 spinner；NoteCard 升级（hover 抬升 3px + accent 封口竖条 + 边框亮起 + 字数 hover 显现 + 操作按钮按压反馈）；时间线分组改为日期胶囊（今天 accent 实心高亮 + 计数徽章 + 渐变分隔线）；卡片进入 stagger 动画（TransitionGroup）。
- 编辑器：书写区 focus-within 纸面微亮（accent 光环）；标题聚焦浮现 accent 下划线；工具栏毛玻璃 sticky + 按钮 hover 上浮/按压缩放；MarkdownViewer 代码块 hover 显示复制按钮（clipboard API）；状态栏时间跟随设置格式。
- 通用组件：Segmented 升级为滑块滑动指示条；Modal backdrop blur 6px；Toast 毛玻璃；EmptyState 信笺+印章插画（hover 微动 + 漂浮点）；Header 搜索框聚焦 accent 光环；Sidebar 折叠态图标补 title。
- 验证：vue-tsc / build / 11 单测全绿。

## 追加：时间线样式完全重构 + 浏览器预览 Mock

- NoteTimeline 完全重构为「纸上时间轴 Letter Timeline」：贯穿渐变竖线（accent 墨痕）+ 封蜡节点（hover 放大辉光）+ 左侧时间刻度列（今天=时刻/昨天/更早=月日，副行互补）+ 里程碑分组（实心圆环大节点 + 粗体标签 + 计数徽章）+ stagger 进入动画 + 窄屏降级。
- 结构：groups 展平为 items 数组（里程碑+信笺），TransitionGroup 单根渲染（规避 <template v-for> 多根 key 编译错误）。
- 新增 services/mock.ts：浏览器 dev 预览内存 Mock（14 条中文种子笔记，覆盖今天/昨天/更早/归档），notes.ts 条件导出（非 Wails + dev 才启用）；桌面端不受影响。

## 追加：编辑书写页完全重构（方案 A 沉浸式纸面，已实施）

用户确认：方案 A 沉浸式纸面 + Markdown 增强 + 全量落地。方案文档 docs/plans/2026-08-28-editor-rewrite-proposal.md。

**新架构**（旧 EditorHeader/EditorToolbar/TagEditor/EditorTextarea 已删除）：
- EditorTopBar：44px 极简顶栏，返回/类型分段或徽标/保存三态圆点(呼吸)/保存按钮/⋯菜单(预览⌘P、专注⌘⇧F、目录、删除)；专注时淡出隐藏。
- EditorBody：无边框正文(16px/1.9) + 选区浮动工具栏(mirror 定位，9 个格式按钮 B/I/S/H1/H2/引用/行内码/链接/代码块，块级工具替换行首标记) + 块级格式提示(行首 #/-/1./>/``` 显示格式胶囊) + Tab 缩进/自动生长。
- EditorTags：轻量标签行（透明输入 + 联想浮层）。
- EditorStatusBar：28px 底部悬浮胶囊，hover/输入显现；字数/阅读时间/目录/专注。
- EditorTocDrawer：预览时右侧悬浮目录抽屉；MarkdownViewer 暴露 scrollToHeading + fill 撑满模式。
- EditorView 编排：viewMode(write/preview)/focusMode 状态机；快捷键 ⌘S/⌘P/⌘⇧F/Esc(专注→预览→返回三级)；⋯菜单删除笔记(带撤销)；纸面聚焦光环保留。

**关键实现点**：浮动工具栏位置用 mirror 元素（同排版隐藏层 + 测量锚点 span 的 getBoundingClientRect）计算选区坐标；块级格式工具做"行首标记替换"。
**验证**：vue-tsc/build/11 单测全绿；浏览器 mock 可完整体验。

## 追加：撤销编辑书写页重构（回滚到沉浸式前）

用户要求撤销刚完成的编辑书写页重构。因改动未提交，git 无法回退，采用"重建旧代码"方式回滚：
- 删除新组件：EditorTopBar/EditorBody/EditorTags/EditorStatusBar/EditorTocDrawer。
- 恢复旧组件：EditorHeader/EditorToolbar/EditorTextarea/TagEditor（原版）。
- EditorView 恢复为沉浸式前版本（常驻工具栏 + 书写/预览/分屏三态 + 聚焦光环 + 标题下划线 + 状态栏）。
- MarkdownViewer 恢复为复制按钮版（去掉 fill/scrollToHeading）。
- i18n 移除新增的 6 个 key。
- 构建 + 11 单测通过。教训：大改前先 git commit 或留备份，否则无法精确回退。

## 追加：交互样式精修（第四轮，撤销编辑器重构后）

- 新增基础组件：v-tip 指令（自定义 Tooltip：毛玻璃+箭头+380ms 延迟+视口翻转，main.ts 全局注册，替代原生 title）、Dropdown（通用下拉：毛玻璃/图标/kbd/分隔/danger，slot 触发）、Switch（替换设置页手写 toggle）。
- 应用：IconButton/EditorToolbar(3 处)/NoteCard(2 处)/AppSidebar 折叠态(3 处) 改 v-tip；Header 新建菜单、FilterBar 排序(原生 select) 换 Dropdown；AppearanceSection 用 Switch。
- useCountUp composable（easeOutCubic 数字滚动），侧栏总笔记/本周统计数字带动画。
- 仪式感：AppHeader 品牌印章入场动画（scale+rotate+spring）；SettingsDrawer 分区 tab 切换淡入上移过渡。
- 验证：vue-tsc/build/11 单测全绿。

## 追加：交互样式精修（第五轮）

- 搜索面板键盘导航：↑↓ 移动高亮（accent 选中态 + 鼠标 hover 联动）、Enter 打开选中项/回车应用筛选、Esc 关闭（Modal closeOnEsc=false 由面板接管）、提示行显示"↑↓ 选择"。
- Toast 升级：类型图标（成功✓/错误✕/警告!/信息i 圆底）、倒计时进度条（animationDuration 由 duration 控制）、进入动画加 scale。
- ConfirmDialog：按类型显示图标（danger 三角/警告感叹号/info 圆点）。
- 时间线「今天」里程碑节点柔和呼吸脉冲（2.6s）。
- 毛玻璃强度 slider：轨道按 --fill 百分比 accent 填充。
- Header 搜索框聚焦微放大（scale 1.03）；编辑器工具栏分组 hover 微亮。
- 验证：vue-tsc/build/11 单测全绿。

## 追加：交互样式精修（第六轮）

- 标签即点即筛：TagChip 加 clickable/select（button 化 + hover 光环），NoteCard 卡片标签点击直接应用为列表筛选（footer 阻止冒泡，归档视图自动回时间流）。
- 时间线：封蜡节点改真实 button 可点击直达编辑（v-tip，hover 放大 1.7）；entry hover 时左侧时间刻度点亮（accent）。
- 滚动感知：HomeView feed 滚动 >8px → Header 浮起阴影（scrolled prop）；>600px 显示返回顶部毛玻璃浮钮。
- 编辑器：工具栏补 Undo/Redo 按钮组；标题 >180 字显示 n/200 计数（>=195 转警告色）；正文字距 0.012em 中文排版微调。
- 验证：vue-tsc/build/11 单测全绿。

## 追加：GitHub Actions CI/CD

用户要求编写 CI/CD 工作流实现自动构建（远程仓库 github.com:sunmking/panda-time-note.git）。

- `.github/workflows/ci.yml`：push/PR main 触发，三平台矩阵（ubuntu-latest / macos-14 / windows-latest）前端 npm ci + vue-tsc/vite build + vitest、go vet、CGO go build 验证（Windows 用 choco 装 mingw），上传二进制 artifact。
- `.github/workflows/release.yml`：tag v* 触发，五平台矩阵（linux amd64/arm64 用 ubuntu-24.04-arm、darwin arm64/amd64 用 macos-13、windows amd64），go install task + wails3 CLI 后复用 Taskfile 打包（linux: deb/rpm/AppImage 分步跳过 aur；darwin: .app+zip；windows: NSIS），softprops/action-gh-release 发布。
- `.github/scripts/set-version.sh`：tag 版本注入 config.yml/nfpm.yaml/darwin Info.plist/wails_tools.nsh，sed 用 -i.bak（BSD/GNU 兼容）、perl 捕获组必须写 ${1}（写 $1 会与后续数字粘连成 $11 组吞掉 key 行）。
- `.github/scripts/collect-artifacts.sh`：产物收集重命名 sujian-<平台>-<架构>-<版本>.<类型>。
- 已验证：YAML 解析 OK、脚本实测通过（版本注入 4 处 + plist lint、三平台产物收集）。


## 追加：整套配色随主题精细化适配

themes.css 重构为主题化架构：
- 每主题定义 --tint/--tint-dark（rgb 三元组）+ accent 四件套。
- 两个通用规则块（:root[data-accent] / :root[data-theme=dark][data-accent]）统一派生：paper-0/1/2 微染主题色（light 2.4-5.2%、dark 4.2-7.5%）、line/line-strong 染色、success/warning/danger 及 soft 与主题色相协调（color-mix 引用 var，特异度 0-3-0 覆盖 dark 基座）。
- base.css 新增 hljs token 主题化配色（keyword=accent-strong、string=success 混合、number=warning 混合、comment=ink-500 斜体等），代码高亮随主题联动。
- 效果：切换墨色时整个软件氛围（纸面/边框/状态色/代码高亮）整体渐变，而非只有点缀色变化。
- 验证：build/11 单测全绿。

## 追加：书写区宽度适配（用户反馈"太窄"）

- 默认书写区宽度 720px → 840px（tokens --content-width）。
- 新增设置项 editorWidth（narrow 660 / medium 840 / wide 1060）：types + settings store（setEditorWidth/apply 设 data-editor-width）+ main.ts/index.html 首帧 + PreferenceSection 分段控件 + 4 语言文案。
- 主题化 width 档位放 themes.css（data-editor-width 覆盖 --content-width）。
- 验证：build/11 单测全绿。

## 追加：自动更新功能（自建，不依赖 Wails 内置 updater）

用户要求"添加自动更新功能"。调研发现 Wails v3 alpha.59 **没有 updater 包**（alpha2/beta 才有），且不能升级 wails 版本（破坏 bindings 兼容），故自建。

- 后端 `backend/updater/updater.go`：Version 变量（ldflags -X 注入）+ Manifest/PlatformAsset/UpdateInfo 结构 + CheckForUpdates（请求 GitHub Releases 的 latest.json + semver 比对）+ Download（带进度回调 + SHA256 校验）+ Apply（跨平台安装：darwin zip 解压替换 .app+脚本重启 / windows 运行 NSIS installer+退出 / linux AppImage 替换重启 或 deb/rpm xdg-open 手动）。
- `backend/handlers/updater_handler.go`：UpdaterHandler service，window 引用由 main.go SetWindow 注入，下载进度通过 window.EmitEvent("updater:download-progress", pct) 推送。
- **bindings 手写不重新生成**：用 @wailsio/runtime 的 Call.ByName（FQN=`sujian/backend/handlers.UpdaterHandler.<Method>`，由 wails3 generator 的 `fqn := pkg.Path + "." + typeName + "." + methodName` 构造），新增 updaterhandler.ts + index.ts 追加导出，完全不动现有 notehandler.ts。
- 前端 services/updater.ts（容错降级，浏览器 dev 不报错）+ UpdateDialog.vue（检查→就绪→下载进度→待安装→安装退出 状态机）+ AboutSection.vue 显示真实版本+检查按钮；i18n 4 语言 settings.updater 文案。
- CI release.yml 增补：build job 设 SUJIAN_VERSION env + 上传 dist/（含 meta）；publish job 用 node 合并各平台 meta.json → latest.json（含 url/sha256/size）上传到 Release。Taskfile 三平台 BUILD_FLAGS 加 `-X sujian/backend/updater.Version=${SUJIAN_VERSION:-dev}`。
- 验证：后端 go build 通过（22M）、前端 vue-tsc 0 错误+build 通过、go build -X 注入 OK、collect meta.json + node latest.json 生成正确。
- 坑：ByName 的方法全名必须用包完整路径（sujian/backend/handlers）不是包名（handlers）；$1 捕获组要写 ${1}（之前 plist 已踩过同样坑）。
