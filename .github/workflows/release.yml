# Release — 自动构建发布
#
# 触发条件：推送 v* 标签（如 v1.0.0）→ 打 tag 即自动发布
#
# 职责：
#   1. 五平台矩阵构建（Linux amd64/arm64、macOS arm64/amd64、Windows amd64）
#   2. 完全复用项目 Taskfile + wails3 CLI 的打包链路（与本地 `task package` 行为一致）：
#        - Linux   → AppImage + deb + rpm
#        - macOS   → .app bundle（adhoc 签名）+ zip
#        - Windows → 可执行文件 + NSIS 安装包
#   3. 版本号从 tag 自动注入（config.yml / nfpm / Info.plist / NSIS）
#   4. 汇总发布到 GitHub Release（自动生成 release notes）
#
# 前置条件：
#   - Go 1.25+ / Node 22+（runner 自带，setup 步骤安装）
#   - wails3 CLI 与 task CLI 由 go install 安装（版本与 go.mod 锁定一致）
#   - Linux AppImage 需要 FUSE（已通过 APPIMAGE_EXTRACT_AND_RUN=1 规避，无需额外依赖）
#   - macOS 产物为 adhoc 签名；如需公证/正式签名，请配置 Apple 证书 secrets 并补充对应步骤
#
# 注意：macos-13 为 Intel (x64) runner，若 GitHub 下线该镜像，可将 darwin-amd64 行改为
#       原生 amd64 runner（如 macos-15-intel）或移除该矩阵项。

name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  GO_VERSION: "1.25"
  NODE_VERSION: "22"

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            os: ubuntu-latest
            package: linux
          - name: linux-arm64
            os: ubuntu-24.04-arm
            package: linux
          - name: darwin-arm64
            os: macos-14
            package: darwin
          - name: darwin-amd64
            os: macos-13
            package: darwin
          - name: windows-amd64
            os: windows-latest
            package: windows
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: go.sum

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # Windows：mingw-w64 gcc（CGO/sqlite3 必需）+ NSIS（安装包）
      - name: Install Windows toolchain
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install mingw -y --no-progress
          choco install nsis -y --no-progress

      # Linux：go install wails3 CLI 编译需 gtk/webkit dev headers；AppImage 需 libfuse
      - name: Install Linux build deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev pkg-config build-essential
          sudo apt-get install -y libfuse2t64 || sudo apt-get install -y libfuse2

      # 安装 task + wails3 CLI（与本地开发工具链一致）
      - name: Install task & wails3 CLI
        shell: bash
        run: |
          go install github.com/go-task/task/v3/cmd/task@latest
          go install github.com/wailsapp/wails/v3/cmd/wails3@v3.0.0-alpha.59
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      # 将 tag 版本（去掉 v 前缀）注入各打包配置文件
      - name: Inject version from tag
        shell: bash
        run: bash .github/scripts/set-version.sh "${GITHUB_REF_NAME#v}"

      # 暴露版本号给后续 task go build（Taskfile 的 BUILD_FLAGS 用 -X 注入 updater.Version）
      - name: Set version env
        shell: bash
        run: echo "SUJIAN_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      # 复用项目 Taskfile 打包（内部含：bindings 生成 → 前端构建 → go build → 平台打包）
      # Linux 拆分为 build + deb/rpm/AppImage，跳过 aur（如需可追加 task linux:create:aur）
      - name: Package (${{ matrix.package }})
        shell: bash
        env:
          APPIMAGE_EXTRACT_AND_RUN: "1"
        run: |
          if [ "${{ matrix.package }}" = "linux" ]; then
            task linux:build
            task linux:create:deb
            task linux:create:rpm
            task linux:create:appimage
          else
            task ${{ matrix.package }}:package
          fi

      # 统一收集产物到 dist/release/（重命名带平台与版本号）
      - name: Collect artifacts
        shell: bash
        run: bash .github/scripts/collect-artifacts.sh "${{ matrix.name }}" "${GITHUB_REF_NAME#v}"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-artifacts-${{ matrix.name }}
          path: dist/
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: release-artifacts-*
          path: artifacts
          merge-multiple: true

      # 合并各平台 meta.json → latest.json（应用内 CheckForUpdates 拉取的更新清单）
      - name: Generate update manifest
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          REPO="$GITHUB_REPOSITORY"
          TAG="$GITHUB_REF_NAME"
          export VERSION REPO TAG
          node -e '
            const fs = require("fs"), path = require("path");
            const metaDir = "artifacts/meta";
            const platforms = {};
            if (fs.existsSync(metaDir)) {
              for (const f of fs.readdirSync(metaDir)) {
                if (!f.endsWith(".json")) continue;
                const m = JSON.parse(fs.readFileSync(path.join(metaDir, f), "utf8"));
                platforms[m.platform] = {
                  url: `https://github.com/${process.env.REPO}/releases/download/${process.env.TAG}/${m.filename}`,
                  filename: m.filename,
                  sha256: m.sha256,
                  size: m.size,
                };
              }
            }
            const latest = {
              version: process.env.VERSION,
              releasedAt: new Date().toISOString(),
              releaseUrl: `https://github.com/${process.env.REPO}/releases/tag/${process.env.TAG}`,
              notes: "",
              platforms,
            };
            fs.writeFileSync("latest.json", JSON.stringify(latest, null, 2));
            console.log("Generated latest.json with", Object.keys(platforms).length, "platform(s)");
          '

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/release/*
            latest.json
          generate_release_notes: true
